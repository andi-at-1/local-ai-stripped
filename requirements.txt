# Python dependencies for the Enhanced Local AI Package Service Manager
# No external dependencies required! Uses built-in Python JSON support.

# The service configuration now uses JSON format (services.json) 
# which is supported natively by Python's json module.

# Optional: For better error handling and validation
# jsonschema>=4.0  # Uncomment if you want JSON schema validation